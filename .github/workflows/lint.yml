name: Lint

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Run Biome lint
        run: pnpm lint

  report-status:
    if: always()
    needs: [lint]
    uses: ./.github/workflows/reporter.yml
    with:
      workflow_name: "Lint"
      conclusion: ${{ needs.lint.result }}
      run_id: ${{ github.run_id }}
    permissions:
      pull-requests: write
      contents: read
